{% extends 'base.html.twig' %}
{% trans_default_domain 'profile' %}

{% block title %}{{ 'index.title'|trans }}{% endblock %}

{% block body %}
    <div class="wrapper">
        <h1>{{ user.nom }} {{ user.prenom }}</h1>
        <p>{{ 'tab.email'|trans({}, 'commons') }} : {{ user.email }}</p>
        <a href="{{ path('app_profile_edit') }}">{{ 'index.edit'|trans }}</a>
        <a href="{{ path('app_panier')}}">{{ 'index.history'|trans }}</a>

    </div>
{% endblock %}
