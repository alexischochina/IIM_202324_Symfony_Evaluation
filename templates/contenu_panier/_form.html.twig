{% trans_default_domain 'cart_content' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default({{ 'btn.save'|trans({}, 'commons') }}) }}</button>
{{ form_end(form) }}
