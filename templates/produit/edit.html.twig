{% extends 'base.html.twig' %}
{% trans_default_domain 'product' %}

{% block title %}{{ 'edit.title'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'edit.title'|trans }}</h1>
    <div class="form">
        {{ include('produit/_form.html.twig', {'button_label': 'btn.update'|trans({}, 'commons')    }) }}
        {{ include('produit/_delete_form.html.twig') }}
        <a href="{{ path('app_produit_index') }}">{{ 'btn.back'|trans({}, 'commons') }}</a>
    </div>
{% endblock %}
